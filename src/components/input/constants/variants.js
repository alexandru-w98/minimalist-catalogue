import hideIcon from "../icons/hide.svg";

const VARIANTS = {
  primary: {},
};

export const TYPES_DEFAULT = {
  password: {
    INPUT_TYPE: "password",
    ICON: hideIcon,
  },
};

export default VARIANTS;
